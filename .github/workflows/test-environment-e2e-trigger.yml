name: E2E Test - Send Environment Event

on:
  pull_request:
    paths:
      - 'publish-release-notes/**'
      - '.github/workflows/test-environment-e2e-*.yml'

jobs:
  send-test-event:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # pin@v4.2.2

      - name: Send dispatch event with environment=prod
        uses: actions/github-script@v7
        with:
          script: |
            console.log('📤 Sending test dispatch event with environment=prod');
            
            await github.rest.repos.createDispatchEvent({
              owner: context.repo.owner,
              repo: context.repo.repo,
              event_type: 'test-environment-verification',
              client_payload: {
                'application-name': 'test-app',
                'product': 'test-product',
                'release-notes': JSON.stringify(['Test: environment verification']),
                'sha': context.sha,
                'title': 'Environment Test Release (prod)',
                'date': new Date().toISOString(),
                'environment': 'prod'
              }
            });
            
            console.log('✅ Dispatch event sent with 7 properties');
